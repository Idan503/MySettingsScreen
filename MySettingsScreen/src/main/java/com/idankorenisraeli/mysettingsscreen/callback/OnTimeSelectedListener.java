package com.idankorenisraeli.mysettingsscreen.callback;

/**
 * Used for returning a new value of a timepickertile with a callback
 */
public interface OnTimeSelectedListener {
    void onTimeSelected(int hours, int minutes);
}
